{% extends "base.html.twig" %}

{% block title %}Pseudo perdu{% endblock %}

{% block body %}

    <div class="container mt-3 col-md-6 col-xs-12">
        <h1 class="mt-3 text-center">Recevoir votre pseudo par e-mail :</h1>
        <form action="{{ path('username_lost')}}" method="POST">
            <div class="form-group">
                <label for="email">Votre e-mail :</label>
                <input type="text" class="form-control" id="email" name="email">
                <button class="mt-3 btn btn-primary">
                    <i class="fas fa-check mr-1"></i>
                    Valider
                </button>
            </div>
        </form>
    </div>
{% endblock %}